import { Component } from '@angular/core';

@Component({
  selector: 'izvor-login-placeholder',
  template: '<p>Login placeholder — Phase 5.2 will replace this</p>'
})
export class LoginPlaceholder {}
